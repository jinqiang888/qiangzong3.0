# 2026-04-03 每日记忆

## 08:59 - 心跳检查
- 检查备份状态：今天已有多次备份（08:08、07:13、06:12、04:42、01:36、01:02、00:38）
- 检测到配置变更：MEMORY.md 和 memory/2026-04-03.md 有未提交的修改
- 变更内容：刚刚修复了火山引擎模型配置问题（删除错误的volcengine provider，保留volcengine-plan）

## 08:53 - 彻底解决GLM-5-Turbo 404错误
**问题**：`glm-5-turbo` 不支持 Coding Plan 功能，导致404错误

**根本原因**：
- 配置中存在两个火山引擎provider冲突：
  - `volcengine-plan`（正确，Coding专用）- endpoint: `/api/coding/v3`
  - `volcengine`（错误，普通模式）- endpoint: `/api/v3`
- 错误的provider导致模型路由失败

**解决方案**：
1. 删除 `volcengine` provider（包含 glm-4-7-251222、doubao-seed-code-preview-251028）
2. 保留 `volcengine-plan` provider（正确的Coding专用配置）
3. 运行 `openclaw doctor --fix` 清理旧配置字段（agents.ids）
4. 重启网关使配置生效
5. 验证网关状态：RPC probe: ok ✅

**现在可用的Coding Plan模型**：
- volcengine-plan/ark-code-latest（默认）
- volcengine-plan/ark-code-latest-32k
- volcengine-plan/ark3.5-long-context
- volcengine-plan/doubao-seed-code
- volcengine-plan/glm-4.7

**经验总结**：
- 已记录到 MEMORY.md 第17条经验教训
- 配置变更会自动触发GitHub备份
- 如需使用其他火山引擎模型，必须通过 `volcengine-plan` provider添加
